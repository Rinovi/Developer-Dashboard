<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"
        integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
</head>

<body>
    <header class="relative bg-white">
        {{!-- Navbar --}}
        <nav aria-label="Top" class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="border-b border-gray-200">
                <div class="flex h-16 items-center">
                    <div class="hidden lg:ml-8 lg:block lg:self-stretch">
                        <div class="flex h-full space-x-8">
                            <div class="flex">
                                <div class="mt-2">
                                    <div class="">
                                        <div class="-mb-px flex space-x-8 px-4" aria-orientation="horizontal"
                                            role="tablist">
                                            <a href="/"
                                                class="border-transparent text-gray-700 text-sm flex-1 whitespace-nowrap border-b-2 px-1 py-4 text-base hover:underline"
                                                aria-controls="tabs-1-panel-1" role="tab" type="button">Home</a>

                                            <a href="/profile"
                                                class="border-transparent text-gray-700 text-sm flex-1 whitespace-nowrap border-b-2 px-1 py-4 text-base hover:underline"
                                                aria-controls="tabs-1-panel-2" role="tab" type="button">Dashboard</a>
                                        </div>
                                    </div>
                                </div>

                                <div class="absolute inset-x-0 top-full text-sm text-gray-500">
                                    <div class="absolute inset-0 top-1/2 bg-white shadow" aria-hidden="true"></div>

                                    <div class="relative bg-white">
                                        <div class="mx-auto max-w-7xl px-8">

                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="flex">
                                <div class="relative flex">
                                </div>

                                <div class="absolute inset-x-0 top-full text-sm text-gray-500">
                                    <div class="absolute inset-0 top-1/2 bg-white shadow" aria-hidden="true"></div>

                                    <div class="relative bg-white">
                                        <div class="mx-auto max-w-7xl px-8">
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="ml-auto flex items-center">
                        <div class="hidden lg:flex lg:flex-1 lg:items-center lg:justify-end lg:space-x-6">
                            {{#if logged_in }}
                            <a href="#" id="logout"
                                class="text-sm hover:underline text-gray-700 hover:text-gray-800">Log
                                out</a>

                            {{else}}
                            <a href="/login" class="text-sm  text-gray-700 hover:text-gray-800">Sign in</a>
                            <span class="h-6 w-px bg-gray-200" aria-hidden="true"></span>
                            <a href="/signup" class="text-sm text-gray-700 hover:text-gray-800">Create
                                account</a>
                            {{/if}}
                        </div>
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    {{{body}}}

    <script src="./homepage.js"></script>
    <script src="./logout.js"></script>

</body>

</html>